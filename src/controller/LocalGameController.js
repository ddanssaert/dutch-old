import BaseGameController from "./BaseGameController";


export default class LocalGameController extends BaseGameController {
    constructor(scene, gameControllerConfig={}) {
        super(scene, gameControllerConfig);
    }
}