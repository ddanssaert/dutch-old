export default class BaseCardSlot {
    constructor(game, x, y) {
        this.game = game;
        this.x = x;
        this.y = y;
    }

    acceptsCard() {
    }

    hasCard() {
    }

    takeCard() {
    }

    putCard(card) {
    }
}